[package]
name = "tunes"
version = "1.0.2"
edition = "2021"
authors = ["Nicholas Manson <nicholaspmanson@gmail.com>"]
description = "A music composition, synthesis, and audio generation library"
documentation = "https://docs.rs/tunes"
homepage = "https://github.com/sqrew/tunes"
repository = "https://github.com/sqrew/tunes"
readme = "README.md"
license = "MIT OR Apache-2.0"
keywords = ["music", "audio", "synthesis", "composition", "midi"]
categories = ["multimedia::audio", "multimedia", "algorithms"]
exclude = [
    ".github/*",
    "*.wav",
    "*.mp3",
    "*.flac",
]

# Optimize dependencies even in debug mode to prevent audio underruns
# This keeps YOUR code debuggable while making dependencies fast
[profile.dev.package."*"]
opt-level = 3

# Release profile: maximum performance for production builds
[profile.release]
opt-level = 3           # Maximum optimization
lto = "fat"            # Link Time Optimization across all crates (big perf win)
codegen-units = 1      # Better optimization at the cost of compile time
strip = true           # Remove debug symbols for smaller binary
# debug = true  # Include debug symbols in release builds (doesn't affect optimization!) but slows compilation; set strip false if this is true

# Alternative: Custom "dev-fast" profile with full optimizations + debug symbols
# Usage: cargo run --profile dev-fast --example synthesis_demo
[profile.dev-fast]
inherits = "release"
debug = true  # Keep debug symbols for debugging

# Live coding profile: fast iteration with good enough audio performance
[profile.live]
inherits = "release"
opt-level = 2           # Slightly less optimization = much faster compile
lto = false             # Disable Link Time Optimization for faster linking
incremental = true      # Enable incremental compilation
codegen-units = 256     # More parallel compilation (faster)

[lib]
crate-type = ["cdylib", "rlib"]


# Feature flags
[features]
default = []
gpu = ["wgpu", "pollster", "bytemuck", "futures-intrusive"]
web = ["cpal/wasm-bindgen"]

[dependencies]
cpal = "0.15"
anyhow = "1.0"
rand = "0.9.2"
lazy_static = "1.4"
midly = "0.5"
notify = "6.1"
crossbeam = "0.8"   # Lock-free structures (epoch-based reclamation for audio callback)
ringbuf = "0.4"     # Lock-free ring buffer for streaming audio
dashmap = "6.1"     # Lock-free concurrent HashMap for sample cache
ahash = "0.8"       # Fast non-cryptographic hashing for cache keys
wide = "0.7"        # SIMD vectorization for DSP performance
rayon = "1.10"      # Data parallelism for multi-core performance
rustfft = "6.2"     # Fast FFT library for convolution reverb

# GPU acceleration (optional - enable with "gpu" feature)
wgpu = { version = "23.0", optional = true }
pollster = { version = "0.3", optional = true }
bytemuck = { version = "1.14", features = ["derive"], optional = true }
futures-intrusive = { version = "0.5", optional = true }

# Audio I/O
hound = "3.5"       # WAV export only (writing)
flacenc = "0.5"     # FLAC export
symphonia = { version = "0.5", features = ["mp3", "isomp4", "aac"] }  # Multi-format import (MP3, OGG, FLAC, WAV, AAC)

[target.'cfg(target_arch = "wasm32")'.dependencies]
getrandom = { version = "0.3", features = ["wasm_js"] }
wasm-bindgen = "0.2"
web-sys = { version = "0.3", features = ["console"] }
console_error_panic_hook = "0.1"

# Benchmarks (run with: cargo bench)
[[bench]]
name = "simd_sample_playback"
harness = false

[[bench]]
name = "simd_wavetable"
harness = false

[[bench]]
name = "simd_effects"
harness = false

[[bench]]
name = "sample_cache"
harness = false

[[bench]]
name = "concurrent_mixing"
harness = false

[[bench]]
name = "export_speed"
harness = false

[[bench]]
name = "polyphony"
harness = false

[[bench]]
name = "streaming_memory"
harness = false

[[bench]]
name = "realtime_latency"
harness = false

[[bench]]
name = "cache_realistic"
harness = false

[[bench]]
name = "gpu_benchmark"
harness = false
required-features = ["gpu"]

[[bench]]
name = "pipeline_benchmark"
harness = false
required-features = ["gpu"]

[[bench]]
name = "realistic_game_audio"
harness = false

[[bench]]
name = "spectral_effects"
harness = false

[[bench]]
name = "gpu_fft_benchmark"
harness = false
required-features = ["gpu"]

[[bench]]
name = "unrealistic_game_audio"
harness = false

[[bench]]
name = "compressed_audio_test"
harness = false

[[bench]]
name = "large_sample_pool_test"
harness = false

# GPU examples require the gpu feature to be enabled
[[example]]
name = "gpu_export_demo"
required-features = ["gpu"]

[[example]]
name = "two_line_gpu"
required-features = ["gpu"]
